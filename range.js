'use strict';

const range = null;
  const range = (a, b) => {
    const arrRange = [];
    for (let i = a; i <= b; i++){
      arrRange.push(i);
    }
    return arrRange;
  };

module.exports = { range };
